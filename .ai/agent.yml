repoName: Featuremachine
mode: pr-only
verbosity: normal

queues:
  tasks: '.ai/backlog/tasks.md'
  bugs: '.ai/backlog/bugs.md'

guardrails: '.ai/guardrails.yml'

hints:
  packageManager: auto
  framework: generic
  testCommand: "echo 'TODO: add tests' && true"
  lintCommand: "echo 'TODO: add linter' && true"
