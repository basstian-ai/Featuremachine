name: Test Agent
on: workflow_dispatch
jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run Agent
        uses: basstian-ai/ai-dev-agent@v1
        with:
          config: .ai/agent.yml
